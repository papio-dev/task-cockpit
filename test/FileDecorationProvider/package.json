{
  "name": "task-cockpit",
  "publisher": "papio-dev",
  "displayName": "Task Cockpit test extension",
  "description": "Task Cockpit test extension",
  "version": "0.0.0",
  "engines": {
    "vscode": "^1.86.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "extension.js",
  "contributes": {
    "commands": [],
    "configuration": {
      "title": "File Decoration",
      "properties": {
        "taskCockpit.decorator.runningSymbol": {
          "type": "string",
          "default": "●",
          "pattern": "^[^\\d\\s]$",
          "markdownDescription": "..."
        },
        "taskCockpit.decorator.overflowSymbol": {
          "type": "string",
          "default": "+",
          "pattern": "^[^\\d\\s]$",
          "markdownDescription": "..."
        },
        "taskCockpit.decorator.badgeOrder": {
          "type": "string",
          "default": "symbolFirst",
          "enum": [
            "symbolFirst",
            "countFirst"
          ],
          "enumDescriptions": [
            "Symbol first",
            "Count first"
          ]
        },
        "taskCockpit.decorator.availableSymbol": {
          "type": "string",
          "default": "•",
          "pattern": "^[^\\d\\s]$",
          "markdownDescription": "..."
        }
      }
    }
  }
}
