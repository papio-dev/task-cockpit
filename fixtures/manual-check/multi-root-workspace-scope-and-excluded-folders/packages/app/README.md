Что проверяет:

- excludeFolders по кастомному имени App (Workspace), по директории docs, tools;
- исключение workspace-scope по имени test (Workspace);
- задачи из workspace-файла .code-workspace;
- dependsOn из одного workspace folder в другой;
- одинаковые label в разных folder’ах;
- группировку по workspace folder.