Что проверяет:

- порядок веток по первому появлению, а не по определению корня;
- задачу-родителя, которая одновременно runnable и группа;
- скрытый промежуточный узел: app:hidden скрыт, но app:hidden:child виден;
- dependsOn на несуществующую задачу;
- composite-задачи без собственного command;
- group как строка и как объект;
- иконки, detail как markdown, tintLabel.