Что проверяет:

- дубли label в одном файле;
- dependsOn на дубль;
- dependsOn на missing/hidden;
- shadowing user ↔ workspace;
- dependsOn на задачу из другого workspace folder;
- предупреждения в Problems и inline-диагностику.