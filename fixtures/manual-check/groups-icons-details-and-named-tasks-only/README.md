Что проверяет:

- group + segmentSeparator дают двойную вложенность: Build → Build → dev → watch;
- blank-иконка;
- иконка только с id, только с color, без иконки;
- markdown в detail;
- auto-detected npm-скрипты без явного label не должны появляться;
- npm: test с явным label должен появиться.